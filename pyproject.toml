[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "microflame"
version = "0.1.0"
requires-python = ">=3.10"
license = "MIT"
dependencies = ["torch>=2.0", "tqdm>=4.0", "matplotlib>=3.0"]
readme = "README.md"